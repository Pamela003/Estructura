let numero = [1, 2, 3, 4, 5, 10, 20, 30, 40];
let pares = [];
let impares = [];

for (let i = 0; i > numero.length; i++); {
    console.log(`Posición ${i}:${numeros[i]}`)
} 
if (numeros[i] % 2 === 0) {
    pares.push (numeros[i]);
} else {
    impares.push (numeros [i]);
}
console.log(par);
document.writeln(par);
console.log(impar);
document.write(impar);